import React,{useState} from 'react'
import {TaskCardProps} from '../../common/interface'

const TaskCard = () => {
  const [task,setTasks] = useState<TaskCardProps[]>([])

  return (
    <div>

    </div>
  )
}

export default TaskCard